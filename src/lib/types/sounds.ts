// ------------------------------
// Alarm & Notification Sounds
// ------------------------------

export interface AlarmSound {
    id: string;   // ej: "beep1"
    name: string; // ej: "Alarma Beep"
    src: string;  // ruta al archivo de audio
  }
  